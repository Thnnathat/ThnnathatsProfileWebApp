pipeline {
  agent any
  stages {
    stage('Get Started Jenkins') {
      steps {
        sh 'echo "Get Started Jenkins"'
      }
    }

    stage('Run Docker') {
      steps {
        sh 'docker run hello-world'
      }
    }

  }
}